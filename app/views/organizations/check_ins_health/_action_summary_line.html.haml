-# Locals: breakdown, popover_html (optional), height_px (optional)
= render "shared/clarity_action_slots/summary",
         breakdown: local_assigns[:breakdown],
         popover_html: local_assigns[:popover_html],
         **local_assigns.slice(:height_px).compact

- content_for :title, "Organizations"

.container
  .row
    .col-12
      .d-flex.justify-content-between.align-items-center.mb-4
        %h1.mb-0 Organizations
        = link_to new_organization_path, class: "btn btn-primary" do
          %i.bi.bi-plus-circle.me-2
          New Organization
      
      - if @current_organization
        .alert.alert-info
          %strong Current Context:
          = @current_organization.display_name
          - if @current_organization.slack_configured?
            %span.badge.bg-success.ms-2 Slack Connected
          - else
            %span.badge.bg-secondary.ms-2 No Slack
      
      - if current_person&.og_admin?
        .row.mb-4
          .col-12
            .card.border-warning
              .card-header.bg-warning.bg-opacity-10
                %h5.mb-0
                  %i.bi.bi-person-badge.me-2
                  Admin: Impersonate User
              .card-body
                = form_with url: impersonations_path, method: :post, local: true, class: "row g-3" do |f|
                  .col-md-8
                    = f.label :email, "Email Address", class: "form-label"
                    = f.email_field :email, class: "form-control", placeholder: "Enter email address to impersonate", required: true
                    %small.form-text.text-muted
                      Enter the email address of the user/teammate you want to impersonate
                  .col-md-4.d-flex.align-items-end
                    = f.submit "Impersonate", class: "btn btn-warning w-100"
      
      .row
        .col-12
          .card
            .card-header
              %h5.mb-0 My Organizations
            .card-body
              - if @organizations.any?
                .table-responsive
                  %table.table.table-hover
                    %thead
                      %tr
                        %th Type
                        %th Name
                        %th Hierarchy
                        %th Slack Status
                        %th Actions
                    %tbody
                      - @organizations.each do |organization|
                        %tr{class: organization == @current_organization ? "table-primary" : ""}
                          %td
                            %span.badge.bg-primary
                              Organization
                          %td
                            = link_to organization_path(organization), class: "text-decoration-none" do
                              %strong= organization.name
                          %td
                            = organization.display_name
                          %td
                            - if organization.slack_configured?
                              %span.badge.bg-success
                                %i.bi.bi-check-circle.me-1
                                Connected
                            - else
                              %span.badge.bg-secondary
                                %i.bi.bi-x-circle.me-1
                                Not Connected
                          %td
                            .btn-group.btn-group-sm{role: "group"}
                              - if organization == @current_organization
                                %span.badge.bg-primary.me-2 Current
                              - else
                                = button_to switch_organization_path(organization), method: :patch, class: "btn btn-outline-primary", form: { style: "display: inline;" } do
                                  %i.bi.bi-arrow-right-circle.me-1
                                  Switch
                              - teammate = @teammates_by_organization_id && @teammates_by_organization_id[organization.id]
                              - if teammate&.follower?
                                = button_to unfollow_organization_path(organization), method: :delete, class: "btn btn-outline-warning", form: { style: "display: inline;" }, data: { confirm: "Stop following #{organization.name}?" } do
                                  %i.bi.bi-heart-fill.me-1
                                  Unfollow
                              - else
                                = link_to edit_organization_path(organization), class: "btn btn-outline-secondary" do
                                  %i.bi.bi-pencil
                                = link_to organization_slack_path(organization), class: "btn btn-outline-info" do
                                  %i.bi.bi-slack
                                = link_to organization_path(organization), method: :delete, class: "btn btn-outline-danger", data: { confirm: "Are you sure you want to delete this organization?" } do
                                  %i.bi.bi-trash
              - else
                .text-center.py-4
                  %p.text-muted No organizations found.
                  = link_to "Create your first organization", new_organization_path, class: "btn btn-primary"
      
      - if @followable_organizations.any?
        .row.mt-4
          .col-12
            .card
              .card-header
                %h5.mb-0 Followable Organizations
                %small.text-muted Organizations with public content you can follow
              .card-body
                .table-responsive
                  %table.table.table-hover
                    %thead
                      %tr
                        %th Type
                        %th Name
                        %th Public Content
                        %th Actions
                    %tbody
                      - @followable_organizations.each do |organization|
                        %tr
                          %td
                            %span.badge.bg-primary
                              Organization
                          %td
                            = link_to organization_path(organization), class: "text-decoration-none" do
                              %strong= organization.name
                          %td
                            .badge-group
                              - if organization.assignments.any?
                                %span.badge.bg-info.me-1
                                  %i.bi.bi-clipboard-check.me-1
                                  Assignments
                              - if organization.abilities.any?
                                %span.badge.bg-success.me-1
                                  %i.bi.bi-award.me-1
                                  Abilities
                              - if @org_ids_with_positions && @org_ids_with_positions.include?(organization.id)
                                %span.badge.bg-warning.me-1
                                  %i.bi.bi-briefcase.me-1
                                  Positions
                          %td
                            = button_to follow_organization_path(organization), method: :post, class: "btn btn-outline-primary btn-sm" do
                              %i.bi.bi-heart.me-1
                              Follow
      
      .row.mt-4
        .col-12
          .card
            .card-header.d-flex.justify-content-between.align-items-center
              %h5.mb-0 Teams & Huddles
              - if @current_organization
                .btn-group.btn-group-sm{role: "group"}
                  = link_to new_huddle_path, class: "btn btn-primary" do
                    %i.bi.bi-plus-circle.me-1
                    New Huddle
                  = link_to organization_teams_path(@current_organization), class: "btn btn-outline-primary" do
                    %i.bi.bi-people.me-1
                    View Teams
            .card-body
              - if @organizations.any?
                - @organizations.each do |organization|
                  - if organization.teams.any?
                    .mb-4
                      %h6.text-muted
                        %i.bi.bi-building.me-1
                        = organization.display_name
                      .table-responsive
                        %table.table.table-sm.table-borderless
                          %thead
                            %tr
                              %th Team
                              %th Huddle Channel
                              %th Huddles
                              %th Recent Activity
                              %th Actions
                          %tbody
                            - organization.teams.each do |team|
                              %tr
                                %td
                                  %strong= team.display_name
                                %td
                                  - if team.huddle_channel_id.present?
                                    %code= team.huddle_channel&.display_name || team.huddle_channel_id
                                  - else
                                    %small.text-muted Not configured
                                %td
                                  %span.badge.bg-secondary= team.huddles.size
                                %td
                                  - if team.huddles.any?
                                    - latest_huddle = team.huddles.max_by(&:created_at)
                                    %small.text-muted
                                      Last: #{time_ago_in_words(latest_huddle.created_at)} ago
                                      - if latest_huddle.huddle_participants.any?
                                        %br
                                        %span.badge.bg-success= "#{latest_huddle.huddle_participants.size} participants"
                                  - else
                                    %small.text-muted No huddles yet
                                %td
                                  .btn-group.btn-group-sm{role: "group"}
                                    = link_to new_huddle_path(team_id: team.id), class: "btn btn-outline-primary btn-sm" do
                                      %i.bi.bi-plus-circle
                                    - if team.huddles.any?
                                      = link_to huddles_path(organization_id: organization.id, team_id: team.id), class: "btn btn-outline-info btn-sm" do
                                        %i.bi.bi-list
                                    = link_to organization_team_path(organization, team), class: "btn btn-outline-secondary btn-sm" do
                                      %i.bi.bi-eye
                              - if team.huddles.any?
                                %tr
                                  %td{colspan: "5"}
                                    .ms-3
                                      %small.text-muted Recent Huddles:
                                      - team.huddles.sort_by(&:created_at).reverse.first(3).each do |huddle|
                                        %br
                                        = link_to huddle_path(huddle), class: "text-decoration-none" do
                                          %i.bi.bi-calendar-event.me-1
                                          = huddle.display_name
                                          %small.text-muted.ms-2= format_date_in_user_timezone(huddle.created_at, format: "%b %d, %Y")
                                          - if huddle.huddle_participants.any?
                                            %span.badge.bg-success.ms-1= huddle.huddle_participants.size
                  - else
                    .text-center.py-3
                      %p.text-muted No teams found in any organization.
                      - if @current_organization
                        %small.text-muted
                          = link_to "Create a huddle", new_huddle_path, class: "text-decoration-none"
                          to get started.
                      - else
                        %small.text-muted Switch to an organization to view teams.
              - else
                .text-center.py-4
                  %p.text-muted No organizations found.
                  = link_to "Create your first organization", new_organization_path, class: "btn btn-primary"

- rows = local_assigns.fetch(:rows)
- title = local_assigns.fetch(:title)
- icon = local_assigns.fetch(:icon)
- casual_name = local_assigns.fetch(:casual_name)
- rating_display_method = local_assigns.fetch(:rating_display_method)
- sentence_type = local_assigns.fetch(:sentence_type)
- nested = local_assigns.fetch(:nested, false)
- heading_tag = nested ? :h5 : :h3

.card.mb-4
  .card-header
    = content_tag heading_tag, class: "mb-0" do
      %i{class: "#{icon} me-2"}
      = title
      %span.badge.text-bg-secondary.ms-2= rows.size
  .card-body
    - if rows.any?
      = render "shared/work_to_meet/table", rows: rows, casual_name: casual_name, rating_display_method: rating_display_method, sentence_type: sentence_type
    - else
      %p.text-muted.mb-0 None in this section.

-# Renders the counterparty completion blurb (completer, object, and observation timeframe).
-# locals: check_in (required), teammate (optional), current_person (optional)
= check_in_counterparty_completion_detail_line(check_in, teammate: local_assigns[:teammate], current_person: local_assigns[:current_person])

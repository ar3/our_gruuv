-# Reusable on-track / off-track pill. Pass status: :good_green | :green | :yellow | :red | :na
-# good_green: dark green "On Track" | green: light green "On Track" | yellow: yellow "On Track"
-# red: red "Off Track" | na: no pill rendered
- opts = on_track_pill_options(local_assigns[:status])
- if opts.present?
  %span.badge.rounded-pill{style: opts[:style], class: opts[:class]}= opts[:label]

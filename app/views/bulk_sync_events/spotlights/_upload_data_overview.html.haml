/ Spotlight Section
.row.justify-content-center.mb-4
  .col-lg-12
    .card.border-0.shadow-sm
      .card-body
        .row
          .col-md-8
            .row.text-center
              .col-3
                .h3.text-primary= @spotlight_stats[:total_syncs]
                %small.text-muted Total Syncs
              .col-3
                .h3.text-success= @spotlight_stats[:completed_syncs]
                %small.text-muted Completed
              .col-3
                .h3.text-warning= @spotlight_stats[:ready_for_preview]
                %small.text-muted Ready for Preview
              .col-3
                .h3.text-info= @spotlight_stats[:recent_syncs]
                %small.text-muted Recent (7 days)
            .row.mt-3
              .col-12
                %h6.text-muted.mb-2 Sync Types
                .row
                  .col-3
                    .d-flex.align-items-center
                      %span.badge.bg-primary.me-2= @spotlight_stats[:assignment_checkins]
                      %small Assignment Check-Ins
                  .col-3
                    .d-flex.align-items-center
                      %span.badge.bg-success.me-2= @spotlight_stats[:employee_uploads]
                      %small Employee Positions
                  .col-3
                    .d-flex.align-items-center
                      %span.badge.bg-info.me-2= @spotlight_stats[:refresh_names]
                      %small Refresh Names
                  .col-3
                    .d-flex.align-items-center
                      %span.badge.bg-warning.me-2= @spotlight_stats[:refresh_slack]
                      %small Refresh Slack
          .col-md-4
            .text-center
              - if @spotlight_stats[:ready_for_preview] > 0
                .alert.alert-warning.small
                  %i.bi.bi-clock.me-2
                  %strong #{@spotlight_stats[:ready_for_preview]} syncs ready for processing
              - elsif @spotlight_stats[:completed_syncs] > 0
                .alert.alert-success.small
                  %i.bi.bi-check-circle.me-2
                  %strong All syncs processed successfully
              - else
                .alert.alert-info.small
                  %i.bi.bi-info-circle.me-2
                  %strong No syncs yet - create your first sync
      .card-footer.bg-light
        .row.align-items-center
          .col-md-6
            %small.text-muted
              %i.bi.bi-funnel.me-1
              Filters: None applied
              %br
              %i.bi.bi-sort-down.me-1
              Sort: Newest first
          .col-md-6.text-end
            %small.text-muted
              %i.bi.bi-table.me-1
              View: Table format
              %br
              %i.bi.bi-eye.me-1
              Spotlight: Bulk Sync Data Overview
